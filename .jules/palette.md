## 2024-05-01 - [ARIA state for toggles]
**Learning:** Icon-only toggle buttons in the PWA require `aria-pressed` or `aria-expanded` in addition to `aria-label` to properly convey active state to screen readers.
**Action:** Always include state ARIA attributes on buttons acting as toggles.
